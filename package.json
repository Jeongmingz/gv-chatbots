{
  "name": "chatbot-skill-server",
  "version": "0.1.0",
  "private": true,
  "description": "Kakao chatbot skill server for Laurastar FAQ",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
